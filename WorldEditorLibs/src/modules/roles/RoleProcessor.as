package modules.roles
{
	import com.zcp.frame.Module;
	
	import common.utils.frame.BaseProcessor;
	
	public class RoleProcessor extends BaseProcessor
	{
		public function RoleProcessor($module:Module)
		{
			super($module);
		}
	}
}